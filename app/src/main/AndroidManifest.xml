<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eshitanarayan.learnandgrow">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

               &lt;!&ndash; <category android:name="android.intent.category.LAUNCHER" />&ndash;&gt;
            </intent-filter>
            -->
        </activity>
        <activity android:name=".SignUp" />
        <activity
            android:name=".Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Resume" />
        <activity android:name=".Feedback" />
        <activity android:name=".GoogleSignin" />
        <activity
            android:name=".ContactUs"
            android:label="@string/title_activity_contact_us"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Aptitude" />
        <activity android:name=".Technical" />
        <activity android:name=".PersonalityDevelopment" />
        <activity android:name=".Quant_sectionActivity" />
        <activity android:name=".Verbal_sectionActivity" />
        <activity android:name=".Reasoing_sectionActivity" />
        <activity android:name=".AboutUs"></activity>
    </application>

</manifest>